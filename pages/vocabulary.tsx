import { NextPage } from "next";
import { useRouter } from 'next/router'
import { useEffect } from "react";
const Vocabulary: NextPage<any> = () => {
    return (
        <div>
            vocabulary
        </div>
    )
}

export default Vocabulary;